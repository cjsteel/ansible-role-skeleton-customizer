---
{% raw %}argument_specs:
  main:
    short_description: "Install and configure {{ role_name }} on your system."
    description: >
      Install and configure {{ role_name }} on your system.
{% endraw %}
    author: {{ custom_role_author }}
{% raw %}    options:
      {{ role_name }}_X:
        type: "str"
        default: "DEFAULT"
        description: "DESCRIPTION"
      {{ role_name }}_Y:
        type: "list"
        default: []
        description: "DESCRIPTION"
      {{ role_name }}_Z:
        type: "bool"
        default: yes
        description: "DESCRIPTION"
{% endraw %}
